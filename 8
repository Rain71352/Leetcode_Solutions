题目:
给你一个字符串 s ，将该字符串中的大写字母转换成相同的小写字母，返回新的字符串。

 

示例 1：

输入：s = "Hello"
输出："hello"
示例 2：

输入：s = "here"
输出："here"
示例 3：

输入：s = "LOVELY"
输出："lovely"
 

提示：

1 <= s.length <= 100
s 由 ASCII 字符集中的可打印字符组成



解答：
class Solution:
    def toLowerCase(self, s: str) -> str:
        return ''.join(chr(asc|32) if 65<=(asc := ord(ch))<=90 else ch for ch in s)
''' "".join()用于将括号中的字符用引号里的符号相连
    |表示按位或(两个元素的ascll编码有一个为1则结果为1)
    asc := ord(ch),ord()返回ascll编码，：=表示海象运算符(在表达式内部给变量赋值)'''
